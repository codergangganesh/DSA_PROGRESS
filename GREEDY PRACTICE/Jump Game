class Solution {
    public boolean canJump(int[] nums) {
        int max_int = 0;
        for (int i =0 ;i<nums.length;i++){
            if (i>max_int)
                return false;
            max_int = Math.max(max_int,i+nums[i]);
        }
        return true;
    }
}